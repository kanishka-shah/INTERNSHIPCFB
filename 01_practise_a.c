#include<stdio.h>

int main()
{
    int length=2;
    int breadth=8;
    int area= length*breadth;
    printf("the length is %d\n",length);
    printf("the breadth is %d\n",breadth);
    printf("the area is %d\n",area);
    return 0;
}
